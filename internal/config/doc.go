// Package config holds local role configuration models, loaders, and startup
// validation rules.
//
// The package is intentionally strict and role-specific. Transitloom config is
// local operator intent plus local runtime settings; it is not a substitute for
// coordinator-managed distributed state.
package config
