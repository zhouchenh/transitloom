// Package identity holds node identity bootstrap scaffolding. It intentionally
// keeps certificate-backed identity distinct from admission-token state so
// later control-session work does not collapse the two concepts.
package identity
